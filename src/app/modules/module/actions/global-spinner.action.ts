import { createAction, props } from '@ngrx/store';

export const showSpinner = createAction('[Global Spinner] Show spinner');

export const hideSpinner = createAction('[Global Spinner] Hide spinner');
